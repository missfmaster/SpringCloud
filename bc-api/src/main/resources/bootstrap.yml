server:
  port: 8000
spring:
  application:
    name: api
  profiles:
    active: default
  cloud:
    config:
      uri: http://127.0.0.1:8001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8002/eureka/